package dao

import (
	"github.com/UnderTreeTech/layout/internal/model"
	"context"
)

// Add the User to the database.
func (d *dao) AddUser(ctx context.Context, s *model.User) (err error) {
	return
}
