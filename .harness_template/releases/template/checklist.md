# 发布检查清单 — v{version}

> **说明**：发布前必须逐项检查，每项完成后打勾。
> 运维按此清单执行，确保发布流程可重复、可审计。

---

## 发布前检查（Pre-Deploy）

### 代码检查
- [ ] 所有 PR 已合并到发布分支
- [ ] CI/CD 流水线全部通过
- [ ] 单测覆盖率达到要求（≥ 80%）
- [ ] 代码审查已完成（2 人 review）

### 三仓检查
- [ ] 业务代码仓分支已就位
- [ ] IDL 契约仓分支已就位（如有 IDL 变更）
- [ ] Harness 仓需求文档已更新

### 数据库检查
- [ ] DDL 变更已在测试环境验证
- [ ] DML 变更已在测试环境验证
- [ ] 数据库备份已完成
- [ ] 回滚 SQL 已准备就绪

### 配置检查
- [ ] 配置变更已在测试环境验证
- [ ] 生产配置已在配置管理系统更新（不提交敏感配置）
- [ ] 配置变更影响面已评估

### 服务依赖检查
- [ ] 上游服务已确认兼容
- [ ] 下游服务已通知变更（如有接口变更）
- [ ] 灰度策略已确认（如需灰度）

---

## 发布步骤（Deploy）

### 1. 执行预部署脚本
```bash
bash releases/v{version}/scripts/pre-deploy/001-xxx.sh
# ... 按序执行所有 pre-deploy 脚本
```
- [ ] 预部署脚本执行完成，无报错

### 2. 执行 DDL 变更
```bash
mysql -h {host} -u {user} -p {database} < releases/v{version}/sql/ddl/001-xxx.sql
# ... 按序执行所有 DDL
```
- [ ] DDL 执行完成，无报错
- [ ] 验证表结构符合预期

### 3. 部署服务
```bash
# 按服务的依赖顺序部署
# 先部署被依赖的服务，再部署上层服务
```
- [ ] 服务部署完成
- [ ] 健康检查通过
- [ ] 关键接口冒烟测试通过

### 4. 执行 DML 变更
```bash
mysql -h {host} -u {user} -p {database} < releases/v{version}/sql/dml/001-xxx.sql
# ... 按序执行所有 DML
```
- [ ] DML 执行完成，无报错
- [ ] 数据验证通过

### 5. 更新配置
- [ ] 更新生产配置
- [ ] 重启相关服务（如需要）
- [ ] 配置生效验证

### 6. 执行后部署脚本
```bash
bash releases/v{version}/scripts/post-deploy/001-xxx.sh
# ... 按序执行所有 post-deploy 脚本
```
- [ ] 后部署脚本执行完成，无报错

---

## 发布后验证（Post-Deploy）

### 功能验证
- [ ] 核心功能冒烟测试通过
- [ ] 本次需求验收标准逐项验证通过
- [ ] 监控告警无异常

### 性能验证
- [ ] 接口响应时间在正常范围（P99 < 200ms）
- [ ] 错误率无明显上升（< 0.1%）
- [ ] 数据库慢查询无明显增加

---

## 发布完成

- [ ] 更新 `requirements/{version}/{requirement-id}/status.json` 为 DONE
- [ ] 发布结果通知相关团队
- [ ] 更新运维文档（如有变更）

---

## 回滚触发条件

如以下任一条件发生，立即执行回滚：
- 核心功能不可用
- 错误率超过 1%
- P99 延迟超过 500ms
- 数据异常

**回滚执行人**：{@运维负责人}
**回滚流程**：参考 `RELEASE_NOTES.md` 中的回滚方案
